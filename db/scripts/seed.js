// ES6 script to seed DB
import dotenv from 'dotenv';

// import connectToDatabase from '../connectionHandler';
// import users from '../seeds/users';

dotenv.config();

const seed = async () => {
  // connect to DB
  // Seed users and other entities here
};

seed();
